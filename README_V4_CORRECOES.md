# Vencimento PA — V4 Correções de navegação

## Correções
- Corrigido o menu superior que acumulava vários listeners a cada `render()`.
- O botão do menu agora usa `onclick`, substituindo o evento anterior em vez de duplicá-lo.
- O cabeçalho voltou a permanecer visível no topo durante a rolagem.
- Mantida a identidade visual profissional e o suporte aos temas claro e escuro.

## Testes
- Verificação de sintaxe do `app.js` com `node --check`.
- Estrutura do ZIP conferida.
